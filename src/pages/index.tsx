import type { NextPage } from "next";
import Table from "@/components/ui/Table";

const Home: NextPage = ({ data }) => {
  return (
    <main>
      <Table />
    </main>
  );
};

export default Home;
